[project]
name = "pipecat-finchvox-tracing"
version = "0.0.1"
description = "A Pipecat example using Finchvox OTEL tracing"
requires-python = ">=3.11,<3.14"
dependencies = [
    "pipecat-ai[daily,webrtc,websocket,silero,cartesia,deepgram,openai,tracing,runner]",
    "finchvox",
]

[tool.uv.sources]
finchvox = { path = "../../..", editable = true }
pipecat-ai = { path = "/Users/derek/projects/pipecat", editable = true }
